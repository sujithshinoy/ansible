- name: play level variables
  hosts: web
  connection: local
  vars_files:
    - course.yaml 
  tasks:
    - name: print variables
      vars:
        Course: Ansible
      ansible.builtin.debug:
        msg: "Course name is {{Course}} and Duration is {{Duration}} and Trainer name is {{Trainer}}"  

  